public enum TokenType {
    ID,
    DIGIT,
    PLUS,
    MINUS,
    TIMES,
    DIVIDE,
    OPEN_PAREN,
    CLOSE_PAREN,
    OPEN_BRACKET,
    CLOSE_BRACKET,
    EQUALS,
    LESS_THAN,
    GREATER_THAN,
    EOF
}
